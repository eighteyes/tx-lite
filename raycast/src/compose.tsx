// compose.tsx
// Raycast entry point for "Compose & Send" (view command)
// Responsibilities: Export default React component as required by Raycast view commands

import { ComposeForm } from "./commands/ComposeForm.js";

export default ComposeForm;
